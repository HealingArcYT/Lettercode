#Import
```import lettercode```
#Objekt erstellen
```lc = lettercode.Lettercode()```
#Objekt mit eigener Liste erstellen:
```lc = lettercode.Lettercode(liste)```
#Buchstabe->Zahl
```lc.code(buchstabe)```
#Zahl->Buchstabe
```lc.decode(zahl)```
